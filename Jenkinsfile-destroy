pipeline {
    agent any
    stages {
        stage('TerraformDestroy'){
            steps{
                //pwd是/var/lib/jenkins/workspace/vprofile-project
                sh 'pwd; cd terraform/; terraform destroy -auto-approve'
                sh 'pwd; cd terraform/; rm -rf *'
            }
        }
    }
}