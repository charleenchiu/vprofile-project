pipeline {
    agent any
    stages {
        stage('Apply'){
            steps{
                //pwd是/var/lib/jenkins/workspace/vprofile-project
                sh 'pwd; cd../; cd terraform/; terraform destroy -auto-approve'
                sh 'pwd; cd../; cd terraform/; rm -rf *'
            }
        }
    }
}